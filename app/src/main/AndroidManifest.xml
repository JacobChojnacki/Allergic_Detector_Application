<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/activity_main_image"
        android:label="@string/app_name"
        android:roundIcon="@drawable/activity_main_image"
        android:supportsRtl="true"
        android:theme="@style/Theme.AllergicDetectorApplication"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.AllergicDetectorApplication.NoActionBar">
        </activity>
        <activity android:name=".SignUp"/>
        <activity android:name=".LogIn"/>
    </application>

</manifest>